# Random-Training-Images-Generation
We construct a random training dataset by generating random noise images and splitting each image into two complementary sources using irregular masks with partial value preservation, so as to evaluate whether the proposed framework depends heavily on semantic image content.
